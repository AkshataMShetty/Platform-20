#include<stdio.h>
#include<stdlib.h>

#define MAX 256


//to copy n character from source to destination
void string_copy(char*,char*,int);

//to convert character to integer
int my_atoi(char*);

//to read user input
char* readinput(char*);

//To append string1 to string2
char* sappend(char*,char*);

//To count string length
int string_length(const char*);

//To append n character from string1 to string2
char *snappend(char *,char*,int);

//To append n character from string1 to string2
char str_cmp(const char*,const char*);

//To reverse a string
char* reverse(char*);

//To insert a character in a given string
char *insertchar(char*,const char,int);

//To count the initial set of characters in buf1 which matches any of the characters in buf2.
int strspn(const char*,const char*);

//To check whether rstr is a rotated string of str
int strrot(const char*,const char*);

//To squeeze the consecutive similar characters in a given string
char *squeeze(char*);

//parses buf until it encounters any of the delimiter in delim
char *strrtok(const char*,const char*);

//To check for the substring sstr in str and if found removes the substring sstr in string str
char *remsstr(char* ,const char*);

//To check the consecutive occurrences of 3 ones.
int check_ones(char*);

//To validate the input to 0 and 1
int validate(char*);

//Program to print vertical histograms of the lengths of words in its inputs
int shistogram(char*);

